// {{project_name}} - Library implementation
#include "{{project_name}}.h"

namespace {{project_name}} {

std::string greet(const std::string& name) {
    return "Hello, " + name + "! From {{project_name}}.";
}

} // namespace {{project_name}}
