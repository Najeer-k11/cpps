// {{project_name}} - Library header
#pragma once

#include <string>

namespace {{project_name}} {

/// Returns a greeting message
std::string greet(const std::string& name);

} // namespace {{project_name}}
